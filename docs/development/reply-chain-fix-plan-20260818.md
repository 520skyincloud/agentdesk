# 回复链路综合修复方案 v2（2026-08-18 15:40，基于其风会话逐条复核重写）

> 状态：待评审。v1 作废——v1 在未读完整聊天记录的情况下先写了方案，本版先证据后方案。
> 证据基线：其风会话 2037-2045 全链路（消息/任务/检索/确认日志/job 表），当前线上 `20260818-promoguard-cc77ebd`。

---

## 0. 其风会话 15:06-15:21 逐条还原（每一步都有库表证据）

| 时刻 | 谁 | 内容 | 系统内部发生了什么（证据） |
|---|---|---|---|
| 15:06:16 | 客户 | 给我办个入住 | turn 559；意图 ✓ hotel_info/checkin_process；知识任务 2236 命中5条；小程序任务 2238 建出 |
| 15:06:25 | AI | "具体要办哪间房呢？" + e秒安心住 | 小程序发出 ✓；但**知识首选证据是"另一间房办不了入住→转接"（检索top1 0.79）**，正确答案"在哪办入住→非前台智能化"（0.83）排第2被挤下；Generate 拿"转接"行当首选证据，回了不着边际的追问 |
| 15:06:36 | 客户 | 管家在吗 | turn 560；检索 top1"有管家吗→转接"（0.81）——**这次是同题，转接是知识库正确指令**；提升→确认 2041 ✓（这一步是对的） |
| 15:06:49 | 客户 | 不要 | 确认分类器模型**又超时**（6081 deadline，日志原文）；关键词兜底判 cancel ✓ 回"好，那先不转人工"（2043）；关掉了管家任务 2239；**但同一消息被意图链分成 hotel_info 任务 2240（pending）落账** |
| 15:06:56 | AI | 又一条确认（2044） | 任务 2240 用"不要"查库→命中"两点前不要打扰我→转接"（0.73）→新提升→新确认（retry 风暴：每秒 dispatch，检索 6 次） |
| 15:21:49 | AI | **没人发消息却冒出第三条确认（2045）** | job 931（消息2042"不要"的 job）15:06:50 创建后一直卡在 intent 阶段重试；**15:21:49 到达 `aiReplyJobLifetime=15分钟` 寿限**，走到"任务超时→`dispatchHuman`（超过等待时限，直接转人工池）"分支，`request_id=ai_reply_job_handoff_931` 发出确认后标记 expired —— **这是纯代码逻辑：超时兜底转人工，与客户意愿无关** |

## 1. 问题定性（三个独立故障，一个数据因素）

### 故障A：异常 FAQ 抢占正常问题的证据首选（知识文字丢失的根因）
- cc77ebd 只拦了"提升人工"，**没拦"进证据集"**
- "给我办个入住"检索：转接行 0.79 排第1、正确答案 0.83 排第2（mixedRecall 排序不看分数绝对值）
- Generate 首选证据是"转接"→ 回复跑偏（"具体要办哪间房"）
- **同题判断是缺失的一环**："办入住"问句与"另一间房办不了"FAQ 不同题，但现状无任何同题校验

### 故障B：确认应答消息被意图链二次处理（反复问的直接根因）
- "不要"同时走了两条路：确认服务（兜底判cancel ✓）+ 意图链（分成 hotel_info 任务 ✗）
- 意图契约 `tasks.minItems=1` 逼模型给纯应答词编任务；提示词零"确认应答"教学
- 取消只关 job 不关账本任务 → 重试 job 从账本捞起"不要"任务再跑
- "不要"查库命中"两点前不要打扰我→转接" → 新确认 → 死循环

### 故障C：15 分钟寿限超时强制转人工（2045 凭空冒出的根因）
- `aiReplyJobLifetime = 15 * time.Minute`；超时走 `dispatchHuman("AI 回复任务超过等待时限")` → 无条件转人工池
- job 931 因故障B 卡在重试；15 分钟一到，**客户早就说了不要，系统反而强制发起转人工**
- 该分支无"客户已明确取消"检查

### 数据因素：混合语义库
- 管家/停车券/退订类"转接"行是**业务正确指令**（该转）
- 但它们与正常问题共享高频词（入住/不要/安静），现状无同题校验就互相误配
- 本版方案**不改知识库数据**（第4点已按你指示搁置），全部靠链路侧同题判定解决

---

## 2. 修复设计（三刀对应三故障，均为代码改动）

### 刀A：同题门禁——"转接"行进入提升或证据集都必须同题（治故障A）

**位置**：`internal/ai/runtime/executor/task_knowledge.go` `buildRuntimeEvidenceBundle`

**规则**（在现有 cc77ebd 守卫外层包一个统一函数 `transferFAQAppliesToTask(task, faq) bool`）：

对"答案含转接"的 FAQ，仅当满足以下任一**同题条件**才允许（1）触发人工提升（2）进入该任务的生成证据集：

1. **实词覆盖**：任务 Query 归一化后（去停用词：的/吗/呢/我/你/在/有/怎么/如何/办/个），剩余实词（≥1个且总长≥2）全部出现在 FAQ 标题中
   - "管家在吗"→实词{管家} → FAQ"有管家吗"含"管家" ✓ 同题
   - "停车券怎么拿"→{停车券,拿} → FAQ"停车券" ✓
   - "给我办个入住"→{给我办个入住归一后=办,入住}…需保留"入住"→ FAQ"另一间房办不了入住"含"入住"——**单字词会误判，故实词取最长名词短语**：入住场景锚词表
2. **锚词表匹配**（兜底，处理中文无分词）：预置业务锚词对 `subIntent锚 → FAQ标题必须含同锚`：
   - 入住/checkin：{办理入住,办入住,怎么入住,入住流程,在哪办入住,入住怎么} ↔ FAQ标题需含"入住"**且不含**否定/异常词（办不了|无法|不能|另一间|两间|手机|删|退）
   - 管家：{管家} ↔ FAQ含"管家"
   - 停车：{停车,车位,停车券,停车费} ↔ FAQ含"停车"
   - 退订/同住人/投诉类：锚=异常词本身（办不了/退/删/两间/手机不能用），**只有 Query 含同样异常词才同题**
3. **Jaccard 兜底**：归一化 bigram Jaccard ≥ 0.22（复用既有 structuredQATopicMatch 阈值）

**不同题时的处理**（治A的关键）：
- 不提升（已有）
- **从该任务的证据集剔除该行**（新增）——让排第2的正确答案上位为首选
- 剔除后若无任何证据 → 按现有 no_context 路径走"资料没写明"（不会发生在这两个case，正确答案都在第2位）

**效果矩阵（全部要写成回归测试）**：

| Query | 检索Top1 | 判定 | 结果 |
|---|---|---|---|
| 给我办个入住 | 另一间房办不了→转接 | 不同题（锚"入住"但FAQ含"办不了/另一间"异常词） | 转接行剔出证据；"在哪办入住"上位 → 知识文字+小程序 |
| 管家在吗 | 有管家吗→转接 | 同题（锚"管家"） | 提升转人工确认 ✓ |
| 停车券怎么拿 | 停车券→转接 | 同题（锚"停车"） | 转人工确认 ✓ |
| 我有两间房另一间办不了入住 | 另一间房办不了→转接 | 同题（Query含"另一间/办不了"） | 转人工确认 ✓ |
| 不要（纯功能词，见刀B） | 两点前不要打扰→转接 | 不进入检索 | — |

### 刀B：确认门禁白名单重排——挂起确认期间，AI 回复 Job 根本不出生（治故障B）

**你指出的问题成立：黑名单思路废弃。** 现有系统其实已有"白名单"设计意图——
`route_state.pending_action == human_handoff` 时，确认服务对客户消息有**独占消费权**
（message_service.go:1203 的 HandleCustomerMessage：分类 confirm/cancel → 消费 →
SkipPendingForMessage → 关任务）。反复问的真因是这个白名单的**实现顺序反了**：

```
现状（错序）：
  消息落库事务内（:1078）→ AI Reply Job 立即创建 ← ★白名单还在 2 秒外的 :1203
  → cron 1 秒内就可能 claim 这个 job 开始跑意图
  → 确认门禁 2 秒后才判完 cancel（模型超时还要等满 2s）
  → SkipPendingForMessage 只能跳过 pending/retry 状态的 job，
    已被 worker claim（processing）的 job 漏网 ← ★洞1
  → 漏网 job 把"不要"跑成 hotel_info 任务落账本
  → 取消只关了当时的任务，漏网 job 后来新建的任务没人关 ← ★洞2
  → job 卡满 15 分钟 → 无条件 dispatchHuman ← ★洞3（刀C 另治）
```

**修复＝把白名单放到 Job 出生之前，并把消费动作做成全量闭合：**

**B1 入队门禁（message_service.go:1077 一处改动）**
- `EnqueueForMessageDB` 之前检查 `route_state.pending_action`：
  - `== human_handoff` → **本次不入队**。确认门禁（事务提交后）对消息分类：
    - confirm / cancel → 消费完毕，**这个消息永远不会有 AI job**（回复由门禁自己发）
    - unknown（如"我要开车出去"这种新主题）→ 门禁调用既有 `EnsureForMessage`（:967 已有此函数）**此时才入队**，AI 正常处理新诉求
  - 无挂起 → 照旧立即入队（99% 的正常消息零额外延迟）
- 效果：挂起确认期间，"不要/取消/确认/随便什么字"**到不了意图链**——不是靠禁词，是靠"没有 job"这个结构性白名单

**B2 消费闭合补强（conversation_handoff_confirmation_service.go，三件事一个事务）**
判 cancel/confirm 消费后，除现有逻辑外：
- 取消该消息已注册的活跃执行（service 已有 `activeExecutions[convID][msgID]` 的 cancel 句柄，现状没人调）
- `SkipPendingForMessage` 覆盖面从 pending/retry 扩到含 processing（漏网兜底）
- 把 `source_message_id=该消息` 的所有非终态任务置 superseded（reason=`consumed_by_handoff_confirmation`，堵洞2）

**B3 意图教学（保留但降级为防御层，无任何词表）**
- 提示词只加一条**状态规则**（不是词名单）：紧邻上一条 AI 消息是转人工确认问题时，当前短消息输出 interaction + requestMode=confirm_previous/cancel_previous；适配层对这两种 requestMode 强制 interaction 并清知识/资源能力
- 因为 B1 已保证挂起期间没有 job，这条只防"确认早已结束、客户隔很久又发'不要'"的历史语境误判；即使误判成 hotel_info，刀A 的同题门禁也让它查不到可提升证据（"不要"与"两点前不要打扰"不同题），双保险

**测试（对应用户场景）**：
1. 挂起确认 + "不要" → 断言：该消息**无 job 行**、无意图 run、无检索日志、只有一条取消回复
2. 挂起确认 + "我要开车出去"（unknown）→ 门禁不消费 → EnsureForMessage 入队 → 正常 AI 回复（不继承转人工语境）
3. 无挂起 + "不要"（隔很久冷发）→ 意图判 interaction；即使误判 hotel_info，同题门禁下无转接提升、无新确认
4. 消费瞬间 worker 已 claim（构造竞态）→ 活跃执行被取消、job 终态 skipped、其新建任务被 superseded

### 刀C：超时转人工前检查客户意愿（治故障C）

**位置**：`internal/services/ai_reply_job_service.go` 超时分支（line ~560）

**改动**：`dispatchHuman(state, job, "AI 回复任务超过等待时限")` 之前增加检查：

```go
// 客户在本 job 生命周期内已明确取消过人工，超时兜底不得再次发起转人工
if route := ConversationRouteService.GetByConversationID(job.ConversationID); route != nil {
    if recentCancel := 已取消标记(见下); recentCancel {
        return AIReplyExecutionResult{Status: Completed, ReasonCode: "expired_after_handoff_cancel"}, errAIReplyJobExpired
    }
}
```

**已取消标记的实现**（不新增表）：在确认服务判 cancel 成功后，往 `t_conversation_event_log` 写一条 `event_type=handoff_cancelled`（含 message_id、时间）；超时分支查该会话**最近一条**该事件，若晚于 job.StartedAt 且其后没有新的客户消息 → 视为客户已拒绝，跳过 dispatchHuman，仅标记 expired。

**测试**：job 卡满 15 分钟 + 期间客户取消过 → 不发转人工、不发确认、job=expired_after_handoff_cancel；未取消过 → 维持原 dispatchHuman 行为（该兜底对真故障仍有价值）。

---

## 3. 不做的事（边界）

- 不改知识库数据（你已指示第4点搁置；刀A的同题判定使数据现状可运行）
- 不引入模型二次评分/重排
- 不改 `aiReplyJobLifetime` 时长本身（15分钟兜底对真卡死仍有意义，只是加意愿检查）
- 不动公开 API/WebSocket/企微协议/计费
- 不改 t_store 权威事实边界

## 4. 实施顺序与工作量

| 序 | 内容 | 预计 | 独立提交 |
|---|---|---|---|
| 1 | 刀B1+B2（白名单重排+消费闭合，先断循环） | 40min | 1个 |
| 2 | 刀B3（意图状态规则+适配，防御层） | 15min | 1个 |
| 3 | 刀A（同题门禁+证据剔除+矩阵测试） | 45min | 1个 |
| 4 | 刀C（超时意愿检查） | 20min | 1个 |
| 5 | 全量测试+构建+部署+清残留 | 20min | — |

顺序理由：先白名单断循环（B1/B2 是同一处改动的两面），再治证据污染（A），B3 防御层随后，C 独立收尾。

## 5. 生产回归验收表（部署后你按行发）

| # | 发 | 必须收到 | 禁止出现 |
|---|---|---|---|
| 1 | 给我办个入住 | 非前台自助办理文字 + e秒安心住两连发 | 追问哪间房/转人工 |
| 2 | 管家在吗 | 转人工确认 | — |
| 3 | （接2）不要 | 好那先不转人工，仅一条 | 任何再确认（含15分钟后） |
| 4 | （接3）隔16分钟不发消息 | 无任何消息 | 凭空确认（2045场景） |
| 5 | 停车券怎么拿 | 转人工确认 | 直接说没有 |
| 6 | 我有两间房另一间办不了入住 | 转人工确认 | 知识编造 |
| 7 | 行李放哪里 | 一楼前台寄存柜 | 资料没写明 |
| 8 | 有没有安静房间 | 避开01/02/11/12 | 问密码锁 |
| 9 | 骂一句 | 接话 | 转人工 |
