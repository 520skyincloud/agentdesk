# 生产回滚至 726b0f3 操作记录（2026-08-18）

> 操作人授权：用户明确指示"直接回滚，保证回滚完全执行，部署服务器，不要只搞一半"。
> 本文档随代码库提交推送，作为不丢失的正式记录。

## 回滚目标

- 发布目录：`/opt/agentdesk/releases/20260815-takeover-responsive-726b0f3`
- 提交：`726b0f3`（2026-08-15 18:06:54 CST，`fix(conversation): keep takeover switch visible on small screens`）
- main 对应合并提交：`c005f90`

## 为什么回滚（决策依据）

1. `726b0f3` 于 8-15 18:12:27 上线后连续无重启运行 41 小时（至 8-17 11:34），是用户确认的行为金标窗口；期间产出多批自然回复（如 8-17 09:13-09:15 的续住/房型/纸笔回复，消息 1573-1585）。该窗口内运行 trace 显示 `intent_tasks.v3 envelope+span normalized`——V3 意图契约在跑且表现良好。
2. 8-17 11:34 `20260817-v3complete-306c50c` 接管后链路断裂（groupKey 协议、技术失败通知、误转人工），此后修复线（ab5cf9e → f631134 → b39f816）未能恢复到金标体验。
3. 用户决定回滚到金标版本，以其为基准重新规划后续。

## 回滚前的完整状态记录

- 当前版本：`20260818-checkinfix-b39f816`（提交 `b39f816`，分支 `codex/reply-chain-complete-fix`，2026-08-18 10:5x 上线）
- 该版本包含的修复（回滚后暂不生效，记录备查）：
  - 流程咨询/执行动作分流 + 办入住/退房恒为知识回答（含"入组"误写）
  - 知识证据治理（每题 4 条上限、公平额度、异常 FAQ 拦截）
  - handoff_claim_validator（无人工任务禁止人工话术）
  - 技术失败不再发客户可见通知（`technical_failure_no_customer_notice`）
- 环境变量（回滚时不改动，与金标窗口一致）：`AI_RUNTIME_MULTIMODAL_V3=on`，CONTEXT/INTENT/REPLY/VALIDATOR=v2，ACTION_LEDGER=authoritative

## 兼容性说明（留有余地）

- 8-17 后新版本 AutoMigrate 增加的列（`t_ai_reply_turn_task.answer_group_key/question_unit_key/answer_requirements_json/requirement_state_json`、`t_ai_reply_job.resolved_coverage_*`、`t_knowledge_retrieve_log` checkpoint 系列列等）**全部保留不删**；726b0f3 的旧模型忽略新列，无影响。
- 回滚窗口期间旧版本写入的行在这些列上为空值；按交接文档 §8.5 的设计，后续切回新版可正常恢复处理。
- 数据库在切换前做全量 mysqldump 快照：`/opt/agentdesk/backups/pre-rollback-726b0f3-<ts>/`。
- 知识库轴不随二进制回滚：8-17 晚重导的 seed-faq（含"答案：转接"行）仍在 FastGPT 数据集中；726b0f3 检索无 topK=5 硬预算（按知识库配置 topK=10），召回更宽，旧内容可进上下文。KB 是否清理另立决策。

## 逃生阀

- 阀 1：`AI_RUNTIME_MULTIMODAL_V3=off` + 重启 → 726b0f3 降为纯 V2 模式。
- 阀 2：软链接切回 `20260818-checkinfix-b39f816`（或 `20260817-voice-v2-ab5cf9e`）+ 重启，10 秒内完成。

## 版本时间线（存档）

```
8-14 16:55  d3265d5（V3 计划动手前最后一颗提交，未单独上过线）
8-15 18:12  726b0f3 上线 ← 本次回滚目标（金标窗口开始）
8-17 11:34  306c50c 接管（金标窗口结束，链路断裂开始）
8-17 15:56  4ae4b10 …
8-17 21:21  ab5cf9e（voice-v2 线）
8-18 10:31  f631134 / 10:5x b39f816（修复线最后版本）
8-18 本次    回滚至 726b0f3
```

## 执行结果

- 操作时间：2026-08-18 11:19–11:21 CST
- 数据库快照：`/opt/agentdesk/backups/pre-rollback-726b0f3-20260818-031946/cs_ai_agent.sql.gz`（3.6MB，SHA256 `cc4e9b43…a7ae505`，gunzip 完整性校验通过；mysqldump 的 PROCESS 权限告警仅涉及 tablespace 元信息，数据完整）
- 目标二进制：SHA256 `ff05e28448e27cee3cedc80e257aa7d62c1a05221ea7d4cbbd3764d992e76dd9`，ELF x86-64 静态 Go，85401298 字节
- 切换：`current` → `releases/20260815-takeover-responsive-726b0f3`，restart 成功（失败自动回切逻辑已备未触发）
- 健康检查：`active` / 首页 200 / 登录页 200 / 无 panic 与 fatal / realtime 客户端正常接入（dashboard_web + admin ws）
- 浏览器实测：登录页正常渲染
- `/opt/agentdesk/REVISION` 已更新为 `726b0f3`

## 回滚后待办

1. 用户以真实会话（其风）验收：入住 / 换房 / 地址 / 优惠 / 外卖。
2. 观察 24 小时：技术失败通知是否为零、误转人工率、回复延迟。
3. 知识库轴独立决策：seed-faq 中"答案：转接"行的清理/改写方案待用户确认。
4. 后续若要在修复线上继续开发，以本记录中的兼容性说明为准（新增列已保留，可直接切回 `b39f816`）。
